import { Response } from 'express';
/* empty array that will contain all users sending a request to server
 * will be used to send responses for each scrape update
 */

export const clients: Response[] = [];
